package main

import (
	"fmt"
	// "math"
	"strconv"
)

func main(){

	// fmt.Println("Hello Operations")

	// price, tax := 275.00, 27.40

	// sum := price + tax
	// difference := price - tax
	// product := price * tax
	// quotient := price / tax

	// fmt.Println(sum, difference, product, quotient)

	// var intVal = math.MaxInt64
	// var floatVal = math.MaxFloat64

	// fmt.Println(2 * intVal)
	// fmt.Println(2 * floatVal)
	// fmt.Println(math.IsInf(floatVal * 2, 0))

	// posResult := 3 % 2
	// negResult := -3 % 2
	// absResult := math.Abs(float64(negResult))

	// fmt.Println(posResult, negResult, absResult)

	// value := 10.2
	// value++
	// fmt.Println(value)
	// value += 2
	// fmt.Println(value)
	// value -= 2
	// fmt.Println(value)
	// value--
	// fmt.Println(value)

	// greeting := "Hello"
	// language := "Go"
	// combinedString := greeting + ", " + language

	// fmt.Println(combinedString)

	// first := 100
	// const second = 200.00

	// fmt.Println(first == second)
	// fmt.Println(first != second)
	// fmt.Println(first < second)
	// fmt.Println(first <= second)
	// fmt.Println(first > second)
	// fmt.Println(first >= second)

	// second := &first
	// third := &first

	// alpha := 100
	// beta := &alpha

	// fmt.Println(second == third)
	// fmt.Println(second == beta)

	// fmt.Println(*second == *third)
	// fmt.Println(*second == *beta)

	// maxMph := 50
	// passaengerCapacity := 4
	// airbags := true

	// fmt.Println(passaengerCapacity > 2 && airbags)
	// fmt.Println(maxMph > 100 || passaengerCapacity == 2)
	// fmt.Println(!(passaengerCapacity > 2 && airbags) && !(maxMph > 100 || passaengerCapacity == 2))

	// kayak := 275
	// soccerBall := 19.50

	// total := kayak + soccerBall
	// total := float64(kayak) + soccerBall
	// total := kayak + int(soccerBall)
	// total := kayak + int(math.Round(soccerBall))

	// fmt.Println(total)
	// fmt.Println(int8(total))

	// val1 := "true"
	// val2 := "false"
	// val3 := "not true"

	// bool1, b1err := strconv.ParseBool(val1)
	// bool2, b2err := strconv.ParseBool(val2)
	// bool3, b3err := strconv.ParseBool(val3)

	// fmt.Println(bool1, b1err)
	// fmt.Println(bool2, b2err)
	// fmt.Println(bool3, b3err)
	
}